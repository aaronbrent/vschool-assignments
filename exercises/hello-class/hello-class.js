function helloClass(className) {
    return "Hello, " + className + " class!";
};

helloClass("V School");

function numDoubler(number) {
    return (number * 2);
};

numDoubler(6);
numDoubler(42);

function favColor (color){
    if (color === "blue"){
        return ("That's my favorite color too!");
    } else {
        return ("What a lame color");
    }
};

favColor("blue");
favColor("red");